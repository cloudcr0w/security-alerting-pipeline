# Example configuration for the AWS Security Alerting Pipeline

# AWS region where infrastructure will be deployed
region = "us-east-1"

# Email address for receiving SNS alerts
alert_email = "your-email@example.com"

# Optional: set project or environment name (used in tags)
project_name = "SecurityAlertingPipeline"
environment  = "dev"
